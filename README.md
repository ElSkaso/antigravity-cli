# Antigravity CLI 🚀

> **The Tool that manages the Framework.**
> A CLI to automate the updating of `PROGRESS.md` and git commits.

## ⚡ Quick Start
1.  `python3 -m venv venv`
2.  `source venv/bin/activate`
3.  `pip install -r requirements.txt`
4.  `python main.py status`

## 📂 Context Order
1.  `MISSION.md` (Why?)
2.  `SPEC_PRODUCT.md` (What?)
3.  `SPEC_TECH.md` (How?)
4.  `STACK_DECISIONS.md` (Constraints)
